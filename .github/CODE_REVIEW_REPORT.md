# Code Review Report: MyMouseBattery
Generated: 2026-05-23T11:31:52.751331

{"type":"result","subtype":"success","is_error":false,"duration_ms":58833,"duration_api_ms":58648,"num_turns":13,"result":"\n\n# Code Review: MyMouseBattery\n\n## CRITICAL Issues\n\n### 1. Multiple NotificationService Instances (Bug)\n**File:** `MagicMouseBatteryApp.swift:25`\n```swift\nprivate var notificationService: NotificationService { NotificationService() }\n```\nEvery access creates a **new** instance. The `@StateObject` on line 8, the MenuBarView on line 98, and SettingsView all get different instances. This means `notifiedDevices` Set is not shared — **notifications will fire repeatedly** because each instance thinks no devices have been notified.\n\n**Fix:** Make it a lazy property or stored property returning the shared instance:\n```swift\nprivate lazy var notificationService = NotificationService()\n```\n\n### 2. Same Issue with LaunchAtLoginService\n**File:** `MagicMouseBatteryApp.swift:26`\n```swift\nprivate var launchAtLoginService: LaunchAtLoginService { LaunchAtLoginService() }\n```\nSame problem — multiple instances with independent `isEnabled` state.\n\n---\n\n## HIGH Issues\n\n### 3. Hardened Runtime Disabled\n**File:** `project.yml:27`\n```yaml\nENABLE_HARDENED_RUNTIME: NO\n```\nHardened runtime should be **enabled** for production apps. This is required for notarization and protects against code injection.\n\n**Fix:** Set to `YES`.\n\n### 4. Notification State Not Persisted\n**File:** `NotificationService.swift:11`\n```swift\nprivate var notifiedDevices: Set<String> = []\n```\nIn-memory only. When the app restarts, previously notified devices will receive duplicate low-battery notifications.\n\n**Fix:** Persist to UserDefaults or use a file.\n\n### 5. `onDevicesUpdated` Never Checks Battery Levels\n**File:** `MagicMouseBatteryApp.swift:35-39`\n```swift\nbatteryService.onDevicesUpdated = { [weak self] in\n    DispatchQueue.main.async {\n        self?.updateStatusItemImage()\n        // Missing: notificationService.checkBatteryLevels(for: batteryService.devices)\n    }\n}\n```\nThe callback updates the UI but never triggers low-battery notification checks.\n\n---\n\n## MEDIUM Issues\n\n### 6. No Authorization Error Handling for Notifications\n**File:** `NotificationService.swift:22-28`\n```swift\nprivate func requestAuthorization() {\n    UNUserNotificationCenter.current().requestAuthorization(options: [.alert, .sound, .badge]) { granted, error in\n        if let error = error {\n            print(\"Notification authorization error: \\(error)\")\n        }\n        // No action if denied\n    }\n}\n```\nIf notifications are denied, there's no user-facing indication or guidance to enable them.\n\n### 7. No Refresh Loading State\n**File:** `MenuBarView.swift:34-45`\nThe \"Actualizar\" button calls `refresh()` but there's no visual feedback while it's in progress.\n\n### 8. IOKit Error Not Logged\n**File:** `BatteryService.swift:48-52`\n```swift\nlet result = IOServiceGetMatchingServices(kIOMainPortDefault, matchingDict, &iterator)\nguard result == KERN_SUCCESS else {\n    return devices  // Silent failure\n}\n```\nFailures are swallowed silently.\n\n### 9. No Input Validation on UserDefaults\n**Files:** `BatteryService.swift:136`, `NotificationService.swift:15-18`\n\nAll UserDefaults reads lack validation. If values are corrupted or unexpected types, behavior is undefined.\n\n---\n\n## MINOR Suggestions\n\n### 10. Hardcoded Interval\n**File:** `MagicMouseBatteryApp.swift:33`\n```swift\nbatteryService.startMonitoring(interval: 60)\n```\n60 seconds is hardcoded. Consider making this configurable.\n\n### 11. Magic Strings\n**File:** `DeviceBattery.swift:27-35`\n`batteryColor` returns string literals (\"green\", \"yellow\", \"red\") instead of a proper enum or Color type, creating a mismatch with SwiftUI's Color expectations.\n\n### 12. Spanish UI Strings\n**Files:** `MenuBarView.swift:13,39,52,68`, `NotificationService.swift:49`, etc.\nMixed Spanish/English codebase. For broader adoption, consider externalizing strings.\n\n### 13. Missing Asset Requirements\n**File:** `MagicMouseBatteryApp.swift:57`\nRelies on `NSImage(named: \"battery\")` existing, but no validation if it's missing.\n\n---\n\n## Summary\n\n| Category | Count |\n|----------|-------|\n| Critical | 2 |\n| High | 3 |\n| Medium | 4 |\n| Minor | 3 |\n\nThe most urgent fix is the **multiple NotificationService instances** — it causes a functional bug where low-battery notifications fire repeatedly instead of once.","stop_reason":"end_turn","session_id":"f6c90ac7-f95a-4890-97b0-cea4797c0d0f","total_cost_usd":0.33270025000000003,"usage":{"input_tokens":28271,"cache_creation_input_tokens":19703,"cache_read_input_tokens":20553,"output_tokens":2317,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":0,"ephemeral_5m_input_tokens":0},"inference_geo":"","iterations":[],"speed":"standard"},"modelUsage":{"MiniMax-M2.7":{"inputTokens":28271,"outputTokens":2317,"cacheReadInputTokens":20553,"cacheCreationInputTokens":19703,"webSearchRequests":0,"costUSD":0.33270025000000003,"contextWindow":200000,"maxOutputTokens":32000}},"permission_denials":[],"fast_mode_state":"off","uuid":"0417d4f8-ae34-4ac2-a2c7-8d6ecabdd90a"}
