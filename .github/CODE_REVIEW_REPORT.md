# Code Review Report: MyMouseBattery
Generated: 2026-05-23T00:03:46.710191

{"type":"result","subtype":"success","is_error":false,"duration_ms":66077,"duration_api_ms":65654,"num_turns":11,"result":"\n\n# Code Review: MyMouseBattery\n\n## 1. CRITICAL Issues\n\n### 1.1 Notifications Never Fire (Bug)\n**File:** `MagicMouseBatteryApp.swift:25` and `:8`\n\nThe `AppDelegate` creates its own `NotificationService` instance via a computed property, but it's separate from the `@StateObject` created in `MagicMouseBatteryApp`. The `BatteryService` callback (`onDevicesUpdated`) only calls `updateStatusItemImage()` — it never calls `notificationService.checkBatteryLevels()`. Notifications are effectively dead code.\n\n**Suggested fix:** Connect `BatteryService` to trigger notification checks. Add a callback or use Combine to pipeline device updates to `NotificationService`.\n\n---\n\n### 1.2 Hardened Runtime Disabled\n**File:** `project.yml:27`\n\n```yaml\nENABLE_HARDENED_RUNTIME: NO\n```\n\nThis is a security risk for a macOS app. Hardened runtime protects against code injection and memory corruption.\n\n**Suggested fix:** Enable it (`ENABLE_HARDENED_RUNTIME: YES`) and fix any entitlements needed (com.apple.security.app-sandbox, com.apple.security.device.bluetooth for IOKit access).\n\n---\n\n### 1.3 Hardened Runtime + No Sandbox\n**File:** `project.yml` / `Info.plist`\n\nThe app has no sandbox and no entitlements. A vulnerability in this app could lead to full system compromise since it uses IOKit (kernel-level access).\n\n**Suggested fix:** If sandboxing, the app would need `com.apple.security.device.bluetooth` entitlement for IOKit. Consider whether sandbox is appropriate or if a minimal entitlement set is better.\n\n---\n\n## 2. HIGH Issues\n\n### 2.1 No Test Coverage\nThe project has zero tests. Given this app interfaces with IOKit and manages system state (notifications, login items), regression risk is high.\n\n### 2.2 Deprecated API Usage\n**File:** `MagicMouseBatteryApp.swift:59-64`\n\n```swift\nresizedImage.lockFocus()\nimage.draw(in: NSRect(origin: .zero, size: resizedImage.size), ...)\nresizedImage.unlockFocus()\n```\n\n`lockFocus`/`unlockFocus` are deprecated in favor of `NSImage.draw(in:from:operation:)` or `drawRepresentation`.\n\n### 2.3 `notifiedDevices` Set Never Purged\n**File:** `NotificationService.swift:11`\n\nWhen a device is no longer present (disconnected), its ID stays in `notifiedDevices` forever. If the same device reconnects with a different UUID, it won't trigger a notification.\n\n### 2.4 No IOKit Error Logging\n**File:** `BatteryService.swift:50-52`\n\n```swift\nguard result == KERN_SUCCESS else {\n    return devices\n}\n```\n\nSilent failure returns empty device list with no logging. Users have no feedback when device detection fails.\n\n### 2.5 `LaunchAtLoginService` Sync Gap\n**File:** `BatteryService.swift:143-147`\n\n```swift\nprivate func syncWithSystemStatus() {\n    if SMAppService.mainApp.status == .enabled {\n        isEnabled = true\n    }\n}\n```\n\nIf `SMAppService` reports enabled, `isEnabled` is set to `true` but `updateLaunchAtLogin()` is **not** called. If the app state is out of sync with the system, the app won't register/unregister properly.\n\n---\n\n## 3. MEDIUM Issues\n\n### 3.1 Missing Image Fallback\n**File:** `MagicMouseBatteryApp.swift:57`\n\n```swift\nif let image = NSImage(named: \"battery\") {\n```\n\nIf the image is missing, `button.image` stays `nil`. No fallback.\n\n### 3.2 Duplicate Service Instances\n**File:** `MagicMouseBatteryApp.swift:24-26`\n\n```swift\nprivate var batteryService: BatteryService { BatteryService.shared }\nprivate var notificationService: NotificationService { NotificationService() }\nprivate var launchAtLoginService: LaunchAtLoginService { LaunchAtLoginService() }\n```\n\nThe `@StateObject` in `MagicMouseBatteryApp` creates separate instances that are never used. This wastes memory and creates confusion.\n\n### 3.3 Popover Recreation on Each Toggle\n**File:** `MagicMouseBatteryApp.swift:113-122`\n\n```swift\n@objc func togglePopover() {\n    if popover.isShown {\n        popover.performClose(nil)\n    } else {\n        if let button = statusItem.button {\n            batteryService.refresh()\n            popover.show(relativeTo: button.bounds, of: button, preferredEdge: .minY)\n            popover.contentViewController?.view.window?.makeKey()\n        }\n    }\n}\n```\n\nNo issue here, but the popover content isn't retained between show/hide cycles — `MenuBarView` state resets each time.\n\n### 3.4 Hardcoded Spanish Strings\n**Files:** Multiple — `MenuBarView.swift:39,52,68`, `NotificationService.swift:49,50`, etc.\n\nNo localization support. Hardcoded Spanish text (\"Actualizar\", \"Configuración\", \"Salir\", \"Batería Baja\") won't adapt to user language.\n\n### 3.5 `eventMonitor` Global Monitor Lifetime\n**File:** `MagicMouseBatteryApp.swift:106`\n\n`NSEvent.addGlobalMonitorForEvents` creates a monitor that lives until app termination (or explicit removal). The `deinit` removes it, but the `AppDelegate` may never be deallocated in a menu bar app's lifetime.\n\n---\n\n## 4. MINOR Suggestions\n\n### 4.1 `DeviceBattery` Equatable by UUID Only\n**File:** `DeviceBattery.swift:3`\n\nThe synthesized `Equatable` compares all fields including `id` (UUID). Two devices with identical properties but different UUIDs (reconnection) won't be equal, which could cause SwiftUI to not recognize them as \"same\" device in `ForEach`.\n\n### 4.2 Missing Authorization Check Before Sending Notifications\n**File:** `NotificationService.swift:22-28`\n\n`requestAuthorization` fires but `checkBatteryLevels` doesn't verify authorization was granted before sending.\n\n### 4.3 Slider Step Mismatch\n**File:** `SettingsView.swift:34`\n\nSlider step is 5, but default threshold is 20. Works fine, just noting the granularity is coarse.\n\n### 4.4 `BatteryService` Timer Retention\n**File:** `BatteryService.swift:26`\n\n```swift\ntimer = Timer.scheduledTimer(withTimeInterval: interval, repeats: true) { [weak self] _ in\n```\n\nThe timer holds a weak reference, but if the target is deallocated the timer won't fire. This is correct behavior, but a timer without a run loop mode defaults to `.common` which may be suppressed during UI events.\n\n### 4.5 No Build Warnings Verified\nThe repo doesn't show any build output. Verify the build completes with zero warnings.\n\n---\n\n## Summary\n\n| Severity | Count |\n|----------|-------|\n| Critical | 3 |\n| High | 5 |\n| Medium | 5 |\n| Minor | 4 |\n\n**The most pressing issue is that notifications are completely broken** — the callback chain from `BatteryService` to `NotificationService` is never established. Beyond that, enabling hardened runtime and adding test coverage would significantly improve the app's security and reliability posture.","stop_reason":"end_turn","session_id":"740eef6b-8196-4e5e-8872-b0e9c89c26f2","total_cost_usd":0.73832025,"usage":{"input_tokens":29373,"cache_creation_input_tokens":19703,"cache_read_input_tokens":76278,"output_tokens":3002,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":0,"ephemeral_5m_input_tokens":0},"inference_geo":"","iterations":[],"speed":"standard"},"modelUsage":{"MiniMax-M2.7":{"inputTokens":65519,"outputTokens":5625,"cacheReadInputTokens":146638,"cacheCreationInputTokens":31485,"webSearchRequests":0,"costUSD":0.73832025,"contextWindow":200000,"maxOutputTokens":32000}},"permission_denials":[],"fast_mode_state":"off","uuid":"4e95f0d4-6b69-4fae-80dc-641a1c844001"}
